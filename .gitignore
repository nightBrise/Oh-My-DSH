# ============ DSH memory runtime data (auto-generated by dsh-memory) ============
# Session checkpoints, MEMORY.md, index, and dream logs — never commit.
.dsh-memory/

# ============ Local tooling / dev artifacts (not for publishing) ============
.mimocode/
.rule-review/
swarm_test.md
swarm_test.py
dsh-webui-enhance/_fix_timers.py

# ============ Design-stage directory (config merged into dsh-agent-swarm) ============
model-router/

# ============ Dependencies ============
node_modules/

# ============ Local private config overrides ============
*.local.yaml

# ============ Logs ============
*.log
dispatch.log
router.log

# ============ OS / editor ============
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
